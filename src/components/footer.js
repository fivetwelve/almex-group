import React from 'react';
import '../styles/footer.scss';

const Footer = () => (
  <div className="footer">
    <p>Almex Group Footer</p>
  </div>
);

Footer.defaultProps = {
  // data: {},
};

Footer.propTypes = {
  // data: articleType,
};

export default Footer;
